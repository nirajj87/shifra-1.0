import type { Context, ReactNode } from "react";
import type { ShifraContext } from "../types";

export const datacontext: Context<ShifraContext>;

export default function UserContext(props: { children: ReactNode }): ReactNode;
